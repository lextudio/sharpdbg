namespace NetFx
{
    public class NetFxClass
    {
        public string GetGreeting(int count)
        {
            var message = "Hello from .NET Framework! Count: " + count;
            return message;
        }
    }
}
